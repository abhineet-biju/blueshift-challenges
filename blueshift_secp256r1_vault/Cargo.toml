[package]
name = "blueshift_secp256r1_vault"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
pinocchio = { version = "0.11.1", features = ["cpi"] }
pinocchio-secp256r1-instruction = { path = "pinocchio-secp256r1-instruction" }
pinocchio-system = "0.6.0"
